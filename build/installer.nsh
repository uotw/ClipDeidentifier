; Custom NSIS include (auto-detected by electron-builder as nsis.include).
; Mark the installer/uninstaller DPI-aware so Windows renders it at native
; resolution instead of bitmap-scaling it (which makes text + icon blurry on
; high-DPI displays).
ManifestDPIAware true
